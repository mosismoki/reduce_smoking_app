<!-- android/app/src/main/AndroidManifest.xml -->
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools">

    <!-- Permissions -->
    <uses-permission android:name="android.permission.INTERNET" />

    <!-- Android 13+ (API 33): اعلان‌ها -->
    <uses-permission
        android:name="android.permission.POST_NOTIFICATIONS"
        tools:targetApi="33" />

    <!-- Android 12+ (API 31): برای setExactAndAllowWhileIdle -->
    <uses-permission
        android:name="android.permission.SCHEDULE_EXACT_ALARM"
        tools:targetApi="31" />

    <!-- اگر BootReceiver ندارید، RECEIVE_BOOT_COMPLETED نیاز نیست -->

    <application
        android:label="reduce_smoking_app"
        android:icon="@mipmap/ic_launcher"
        android:enableOnBackInvokedCallback="true">

        <!-- Flutter V2 embedding -->
        <activity
            android:name=".MainActivity"
            android:exported="true"
            android:launchMode="singleTop"
            android:theme="@style/LaunchTheme"
            android:configChanges="orientation|keyboardHidden|keyboard|screenSize|smallestScreenSize|locale|layoutDirection|fontScale|screenLayout|density|uiMode"
            android:hardwareAccelerated="true"
            android:windowSoftInputMode="adjustResize">

            <meta-data
                android:name="io.flutter.embedding.android.NormalTheme"
                android:resource="@style/NormalTheme" />

            <intent-filter>
                <action android:name="android.intent.action.MAIN" />
                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>

        <!-- اعلام استفاده از V2 -->
        <meta-data android:name="flutterEmbedding" android:value="2" />

        <!-- یادآور «وقت سیگار»؛ فقط داخل اپ (AlarmManager -> Notification) -->
        <receiver
            android:name=".notifications.ReminderReceiver"
            android:enabled="true"
            android:exported="false" />

        <!-- اکشن‌های Smoke / Skip و پایان پنجره؛ فقط داخل اپ -->
        <receiver
            android:name=".notifications.ActionReceiver"
            android:enabled="true"
            android:exported="false">
            <!-- اختیاری ولی مفید: فیلتر اکشن‌ها برای سازگاری بیشتر -->
            <intent-filter>
                <action android:name="SMOKE_ACCEPT" />
                <action android:name="SMOKE_SKIP" />
                <action android:name="WINDOW_TIMEOUT" />
            </intent-filter>
        </receiver>

    </application>
</manifest>
